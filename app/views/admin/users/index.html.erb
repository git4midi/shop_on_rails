<%= render partial: 'users_submenu', locals: {buttons: [:show_all, :add_new]} %>
<h1>Все пользователи</h1>
<% if @users.empty? %>
  <b>В базе нет пользователей</b>
<% else %>
  <table class='noborders'>
    <tr>
      <td><%= render partial: 'users_management_table' %></td>
    </tr>
  </table>

<% end %>
