<% buttons = [:show_all] %>
<% buttons << :add_new unless params[:category_id].nil? %>
<%= render partial: 'items_management_submenu', locals: {buttons: buttons} %>
<table class='noborders'>
  <tr>
    <td class='expand'>
      <table class='noborders expand'>
        <tr>
          <td><%= render partial: '/selected_category' %></td>
          <td class='items-count-pagination'><%= items_count_pagination_for_items_managements_items %></td>
        </tr>
      </table>
      <%= render partial: 'items_management_table' %>
      <hr>
      <%= pages_pagination_for_items_management_items %>
      <hr>
    </td>
    <td><%= render partial: '/categories_menu_table', locals: {count_this: :my_items, path_proc: lambda{|c| items_management_category_items_path(c)} } %></td>
  </tr>
</table>